# PostgreSQL Database Connection
# Copy this file to .env.local and fill in your actual database credentials

# Database URL format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://username:password@localhost:5432/content_analytics?schema=public"

# Optional: Direct URL for migrations (if using connection pooling)
DIRECT_URL="postgresql://username:password@localhost:5432/content_analytics?schema=public"

# Application Settings
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
